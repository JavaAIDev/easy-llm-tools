package {{packageName}};

import {{specPackageName}}.{{toolFactoryClassName}};
import {{generatedSpecPackageName}}.{{configurationClassName}};
import {{generatedSpecPackageName}}.{{parametersClassName}};
import {{generatedSpecPackageName}}.{{returnTypeClassName}};

public class {{toolClassName}}Factory implements {{toolFactoryClassName}}<{{parametersClassName}}, {{returnTypeClassName}}, {{toolClassName}}{{#if hasConfiguration}}, {{configurationClassName}}{{/if}}> {

    @Override
    public {{toolClassName}} create(final {{configurationClassName}} config) {
        return new {{toolClassName}}(config);
    }

    @Override
    public String toolId() {
        return "{{#escapeJava definition.id}}{{/escapeJava}}";
    }
}
